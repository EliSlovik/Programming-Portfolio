class Box:
  len = 0.0
  wid = 0.0
  hei = 0.0
  def __init__(self):
    self.len = 0.0
    self.wid = 0.0
    self.hei = 0.0
  def volume(self):
   return self.len * self.wid * self.hei
  def surfaceArea(self):
    return 2 * (self.len * self.wid + self.len * self.hei + self.wid * self.hei)
